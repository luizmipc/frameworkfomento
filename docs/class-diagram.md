# Diagrama de Classes

> Dono: `dev`. Mantido via `/kanban-sync` e `/kanban-start`.

_A preencher — nenhum model Django existe ainda. Quando houver, descrever em
Mermaid (` ```mermaid `/`classDiagram`)._

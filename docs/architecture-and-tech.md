# Arquitetura e Tecnologias

> Dono: `dev`. Mantido via `/kanban-sync` e `/kanban-start`.

_A preencher. Estado atual conhecido: Django 6.0.7 gerenciado por `uv`
(`app/pyproject.toml`/`app/uv.lock`), containerizado via `Dockerfile`
(raiz) + `docker-compose.yml`, sem apps Django reais ainda além do scaffold
padrão (`config/settings.py|urls.py|wsgi.py|asgi.py`)._

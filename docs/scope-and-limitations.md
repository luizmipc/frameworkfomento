# Escopo e Limitações

> Dono: `product-owner`. Consultado por `/kanban-sync` para detectar desvio
> de escopo em tarefas novas ou existentes.

_A preencher — nenhum escopo/limitação documentado ainda. Enquanto vazio,
`/kanban-sync` não deve bloquear nenhuma tarefa por falta de referência._

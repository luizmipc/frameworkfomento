[project]
name = "app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django==6.0.7",
    "gunicorn>=26.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.1",
    "bandit>=1.8.0",
    "semgrep>=1.170.0",
]
